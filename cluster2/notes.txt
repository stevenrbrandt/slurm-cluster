What is going on with a node in my swarm?
    docker node ls
    docker node inspect --pretty et-cluster_slurm-jupyter
    docker service ls
    docker service ps et-cluster_slurm-node2 --no-trunc

munge is very important to get right or slurm won't work. For this purpose, relink.sh now distributes the munge key as well as passwd, group, and shadow.

If a user's login gets corrupted and they can't get on the system:

  jupyter lab --generate-config

Should fix things.

The command

  docker swarm join-token worker

Will spit out a command that can be used by worker nodes to join the swarm.
The "head node" will have to do this:

  docker node update --label-add node.type=head etk1.cct.lsu.edu

Unresponsive node fix:
  
  On the node: docker swarm leave && docker swarm join --token SWMTKN-1-3yob54agu... 130.39.21.211:2377
  On the head node:
    docker node ls # identify and remove inactive copies of nodes
    docker node update --label-add node.type=node3 etk4.cct.lsu.edu # relabel the node

Note that docker swarm networking usually gives every node 2 ip addresses, a good one and a bad one. We have to build our own /etc/hosts file dynamically at runtime using relink.sh to limit ourselves to the good addresses.

The slurm.conf is in /home/etuser/slurm.conf. Edit it there to exactly match the nodes in production, then restart with r0.sh.

Docker buildx:

